git@github.com:ssbandjl/beegfs_v715.git

编译:
make -C admon/build admon_gui

问题: Error :Arc dependent binaries in noarch package (CentOS 6.5)
添加如下定义到spec中
%define _binaries_in_noarch_packages_terminate_build   0

build:
clear;make package-rpm PACKAGE_DIR=packages BEEGFS_VERSION=7.1.5-local1




__IBVSocket_waitForRecvCompletionEvent
    ibv_poll_cq


IBVSocket_checkConnection

__IBVSocket_postWrite
    ibv_post_send


admon-gui
条带设置: stripe settings
actionPerformed
String url = HttpTk.generateAdmonUrl("/XML_Striping")
parser.setUrl(url + params)
...
改变 change

cpp
int WebServer::requestHandler(struct mg_connection *conn) -> http服务端
...
    if (strcmp(requestInfo->uri
    ...
    else if (strcmp(requestInfo->uri, "/XML_Striping") == 0)
    xmlhelper::striping(conn, requestInfo, postData, postDataLen) -> void xmlhelper::striping
        NodeStoreMgmtEx* mgmtNodes = app->getMgmtNodes()
        bool setPatternRes = managementhelper::setPattern
...



